# Ollama models are discovered at runtime from GET /api/tags on the configured
# Ollama instance. Add entries here only to override or enrich specific models
# (pricing, supported_parameters, context_length, enabled: false, etc.).
models: []
